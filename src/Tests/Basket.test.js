import React from 'react';
import { render, fireEvent, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import Front from '../Frontend/Front';

test('Clicking Product A button updates the total price', () => {
    //Starting test here.
});
